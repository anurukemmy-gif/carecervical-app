import streamlit as st

# ตั้งค่าหน้าตาแอป
st.set_page_config(page_title="CareCerv", page_icon="🎗️")

# ส่วนหัวของแอป
st.title("🎗️ CareCerv: แอปฯ มั่นใจ ไกลมะเร็ง")
st.subheader("ดูแลสุขภาพผู้หญิง ง่ายๆ ด้วยตัวคุณเอง")

# เมนูหลักแบบ Tab
tab1, tab2, tab3 = st.tabs(["📊 ประเมินความเสี่ยง", "🧪 วิธีเก็บตัวอย่าง", "📍 จุดตรวจใกล้บ้าน"])

with tab1:
    st.header("แบบประเมินเบื้องต้น")
    q1 = st.radio("1. คุณมีอายุระหว่าง 30-60 ปีหรือไม่?", ["ใช่", "ไม่ใช่"])
    q2 = st.radio("2. คุณเคยตรวจคัดกรองมะเร็งปากมดลูกในช่วง 5 ปีที่ผ่านมาไหม?", ["เคย", "ไม่เคย"])
    
    if st.button("ดูผลประเมิน"):
        if q2 == "ไม่เคย":
            st.warning("คุณควรได้รับการตรวจคัดกรองนะครับ ปัจจุบันมีแบบเก็บตัวอย่างเองที่บ้านแล้ว!")
        else:
            st.success("เยี่ยมมากครับ! รักษาสุขภาพและตรวจตามนัดต่อไปนะ")

with tab2:
    st.header("ขั้นตอนการเก็บตัวอย่างด้วยตนเอง")
    st.info("ทำตาม 3 ขั้นตอนง่ายๆ ดังนี้:")
    st.write("1. **ล้างมือให้สะอาด** และหาท่าที่ผ่อนคลาย (นั่งยองหรือยืนยกขา)")
    st.write("2. **ใช้ไม้เก็บตัวอย่าง** สอดเข้าไปและหมุนตามคำแนะนำในคู่มือ")
    st.write("3. **เก็บไม้ใส่หลอด** และส่งคืนที่ รพ.สต. หรือ อสม.")
    # จำลองการใส่ภาพหรือวิดีโอ
    st.image("https://via.placeholder.com/400x200.png?text=Video+Tutorial+Click+Here")

with tab3:
    st.header("ค้นหาจุดบริการใกล้คุณ")
    location = st.text_input("ระบุชื่ออำเภอหรือตำบล")
    if location:
        st.write(f"📍 พบจุดตรวจใกล้ {location}: รพ.สต. มิตรภาพ, รพ.ประจำอำเภอ")
        st.button("โทรนัดหมายด่วน")

# ส่วนท้าย
st.divider()
st.caption("สนับสนุนโดย ทีมวิจัยเพื่อสุขภาพชุมชน")
